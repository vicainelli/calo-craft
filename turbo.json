{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [
    "**/.env.*local"
  ],
  "tasks": {
    "build": {
      "outputs": ["dist/**", ".next/**"],
      "dependsOn": ["^build"]
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true,
      "inputs": [
          "apps/**/*.ts",
          "apps/**/*.tsx",
          "apps/**/*.js",
          "apps/**/*.jsx",
          "tsconfig.json",
          "tsconfig.*.json",
          "vite.config.ts",
          "package.json"
      ],
      "env": [
          "NODE_ENV",
          "PORT",
          "VITE_*"
      ]
    },
    "test": {}
  }
}
